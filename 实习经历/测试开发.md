# 测试开发岗 · 实习经历（可贴简历）

南京拾越信息科技有限公司｜测试开发实习生｜2026.08–至今  
负责华硕新商城、华硕小二测试交付与接口自动化落地。

- 自建 pytest + requests 接口框架：用 Chrome CDP 读取已登录 token，conftest 注入 admin/seller 客户端；登录缺失则 skip 不误报。用例按 smoke / api / write 分层，新需求只加 test_*.py。
- 把用例交付做成流水线：源 JSON 为唯一数据源，一次生成禅道 12 列 CSV、查看版、执行状态清单、执行手册；环境入口、登录隔离、数据台账、证据目录写成可重复执行的规则。
- 偶现问题脚本化：活动编辑店铺空白做多轮时序记录与强制延迟对照，定位为接口竞态；修复后按轮次统计回归。业务侧同步完成多端功能测试与缺陷闭环。

## 技能栏（本岗）

Python、pytest、requests、HTTP/REST、Token/Session、Git、Linux、功能测试。

不要写：Allure、Jenkins、PO 模式、UI 自动化平台。仓库里还没做到那一步。
